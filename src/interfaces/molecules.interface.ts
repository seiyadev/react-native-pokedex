import { ImageStyle, TextStyle, ViewStyle } from 'react-native';

export interface EditProfileFormStyle {
  container: ViewStyle;
  form: ViewStyle;
  imagecontainer: ViewStyle;
  image: ImageStyle;
  textinput: TextStyle;
  datepicker: ViewStyle;
  text: TextStyle;
  errortext: TextStyle;
  buttoncontainer: ViewStyle;
  button: ViewStyle;
}
